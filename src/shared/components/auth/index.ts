/**
 * Auth Guards - Shared components for route protection
 * Best practice: Shared across entire app, not feature-specific
 */

export { ProtectedRoute } from "./ProtectedRoute";
export { PublicRoute } from "./PublicRoute";
